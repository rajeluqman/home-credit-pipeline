# Infra Limits Log — Home Credit Risk Pipeline

> Owner: @infra-reality-agent. Every entry is an OBSERVED or PROJECTED ceiling with the actual
> number, not a guess. Cross-check any resourcing proposal against this before approving.

| Date | Resource | Ceiling | Source | Status |
|---|---|---|---|---|
| 2026-06-28 | Glue executor memory | G.1X × 2 workers ≈ 32 GB total | README.md "Stack" table + AWS Glue G.1X spec (1 DPU = 4 vCPU/16GB) | Projected — sizing math in `docs/ADR/ADR-003-kimball-over-obt-sizing.md`, not yet measured against a real cloud run |
| 2026-06-28 | S3 free-tier storage | 5 GB | `.env.example` `S3_BUCKET_DEV` etc. comment | Tight against Landing+Bronze+Silver copies of 7 CSVs at full scale (58.4M rows); dev mode uses 1,000-row samples to stay under it |
| 2026-06-30 | S3 actual usage, `home-credit-risk-dev-1` (Observed, Phase 2 scoping) | 5 GB account-wide free tier | `boto3 s3.list_objects_v2` paginated over full bucket, this session | **Observed: 2.6718 GB / 75 objects** — `landing/` 2.6571 GB (7 objects = the full 58.4M-row raw CSVs, already landed from the Phase-1 Step-8 download, never deleted per Flow B) + `bronze/` 0.0071 GB (28 objects, sample-only) + `silver/` 0.0075 GB (40 objects, sample-only). Already 53% of the 5 GB ceiling from raw alone — landing a second staging-bucket copy + full-scale Bronze/Silver Delta output will need a real post-Bronze size check before assuming it fits, not an assumption. |
| 2026-06-30 | S3 staging/prod buckets (Observed) | — | `boto3 s3.head_bucket` + `list_objects_v2`, this session | `home-credit-risk-staging` and `home-credit-risk-prod` **already exist** (provisioned earlier, not by this session) and are **empty** (0 objects each) — confirmed live, not assumed from `.env.dev` naming alone. |
| 2026-06-30 | Glue execution IAM role (Observed — blocking finding) | — | `boto3 iam.list_roles()` full enumeration (8 roles total), this session | **None exists.** Only Snowpipe/Snowflake-related roles (`snowflake_silver_loader`, `snowflake-s3-gold-reader`, `creative-intel-snowflake-role`) + unrelated `AmazonMWAA-novartis-pharma-mwaa-*`/`creative-intel-ci-role` + 3 AWS service-linked roles. **A real (non-Docker) Glue job cannot run at all until a Glue execution role is created** — new IAM role creation, owner-executed per the same precedent as `snowflake_silver_loader` (`PROJECT_STATUS.md` "AWS admin credential" entries). |
| 2026-06-30 | Glue jobs registered in account (Observed) | — | `boto3 glue.get_jobs()`, this session | **0 jobs.** Nothing has been created in AWS Glue yet for this project — Phase 2 starts from zero on the Glue side, not just "point existing jobs at full data." |
| 2026-06-30 | Bronze full-scale `bureau_balance` (27,299,925 rows) local Spark ingest (Observed) | 7.8 GB total / ~6.0 GB available / no swap (this Codespace) | `time python bronze/ingest_bronze.py --table bureau_balance --env staging`, this session | **No OOM.** 27,299,925 rows exact match, 0 quarantine, 2m32s wall time, `free -h` showed ~6.0 GB available both before and after (no leak). **First real positive signal**, though scoped honestly: this is Bronze (flat read CSV → add 2 cols → write Delta, no joins/window-fan-out), not the Silver-layer Glue job ADR-003's math was modeling, and not real AWS Glue (local PySpark+S3A connector, see `PROJECT_STATUS.md` Phase 2 entries) — does not by itself close the Gate-2 "real AWS Glue, no OOM" checklist item, which is specifically about the Silver step. |
| 2026-06-30 | Glue OOM risk, full-scale `bureau_balance` (27M rows), real AWS Glue (Observed — RESOLVED) | G.1X × 2 workers ≈ 32 GB total executor memory (ADR-003 budget) | `glue.start_job_run("glue_silver_bureau")` + `glue.get_job_run()` polled to completion, this session | **No OOM.** `glue_silver_bureau.py` processed `bureau` (1,716,428 rows) + `bureau_balance` (27,299,925 rows) on real AWS Glue, G.1X×2, **SUCCEEDED in 96s, 192 DPU-seconds.** Output verified: `silver_bureau` 1,716,428 rows (100% passthrough), `silver_bureau_balance` 610,965 rows (post `MONTHS_BALANCE=0` filter + dedup). This is the real cloud signal the row below was waiting for — superseded, not duplicated. |
| ~~—~~ | ~~Glue OOM risk, full-scale `bureau_balance` (27M rows) join~~ | ~~Unconfirmed~~ | ~~Phase 4b cloud promote~~ | **RESOLVED 2026-06-30 — see row above.** Original entry (2026-06-28) read "Open — first real signal will come from the Phase 4b run, not before"; that run happened this session. |

## How to add an entry
Only add a row with a REAL observed number (a Glue job's CloudWatch metrics, an actual S3
bucket size, a Snowflake warehouse credit report) or a clearly-labeled "Projected" estimate
with its source formula shown. Never round up a guess into looking like a measurement.
